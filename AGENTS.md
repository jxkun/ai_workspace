# Agent 协作指南

本文件是 `ai_workspace` 的根级协作约定，适用于整个 workspace。

如果子目录中存在自己的 `AGENTS.md`，以离目标文件最近的那份为准；根文件只提供默认规则。

## 角色定位

这个仓库是 AI 学习、实验和项目沉淀空间。Agent 在这里工作时，应优先保证内容可读、可复现、可继续维护，并把具体学习工程或项目收束到 `project/<name>/` 这样的独立目录中。

## 通用原则

- 默认使用中文沟通和编写文档。
- 保持改动聚焦，不做与当前任务无关的整理、迁移或重命名。
- 先看目录内已有 README、AGENTS 和文档，再新增规则或代码。
- 新增内容要放到职责匹配的目录，不要把根目录变成临时文件堆。
- 根目录只维护 workspace 级索引、规则和跨项目说明；具体学习工程、实验、项目代码和项目内资料应落到对应 `project/<name>/`。
- 不提交真实 API Key、私有 Token、Cookie、生产数据或个人敏感信息。
- 大文件、生成物、缓存和临时数据默认不纳入版本管理，除非它们本身就是文档化资产。

## 目录约定

- `docs/`：跨项目通用资料、学习路线、框架对比、概念笔记；不放只服务于单个项目的实现细节。
- `project/`：只放具体学习工程或项目；每个一级子目录 `project/<name>/` 都应代表一个明确主题，是独立、可运行或可持续演进的单元。
- `repo/`：外部源码、研究对象或临时代码副本；不作为本 workspace 的项目实现目录。

`project/` 不作为主题分类目录、通用资料库或外部源码镜像区；其下不要直接堆放散文件、跨项目资料、一次性草稿或外部源码。此类内容应分别进入具体 `project/<name>/`、根 `docs/`、临时目录或 `repo/`。子项目内部目录以子项目自己的 README 或 AGENTS 为准。

## 新增项目约定

在 `project/<name>/` 下新增项目时，先确认 `<name>` 对应一个具体学习主题、实验工程或项目，而不是宽泛分类或资料集合。项目目录应尽量自包含，具备清晰目标、运行入口或持续演进计划，优先补齐：

- `README.md`：项目目标、目录结构、运行方式、当前状态。
- `AGENTS.md`：该项目内 Agent 的协作规则、边界和测试要求。
- `.gitignore`：项目级缓存、密钥、构建产物忽略规则。

按需要再添加：

- `docs/`：项目设计、路线图、学习笔记。
- `examples/`：可运行示例。
- `scripts/`：本地辅助脚本。
- `src/`：可复用代码。
- `tests/`：测试。
- `configs/`：配置模板，不放真实密钥。
- `data/`：小规模样例数据或可复现测试数据。

如果只是补充跨项目学习路线、框架对比或通用概念笔记，优先放到根 `docs/<topic>/`；如果只是研究第三方源码、保存外部仓库副本或临时克隆仓库，优先放到 `repo/`。

## 文档约定

- 文档标题应清楚表达主题，不使用过宽泛的名字。
- 学习路线类文档要说明目标、前置基础、阶段、练习和推荐产出。
- 设计类文档要说明目标、约束、方案、取舍、验证方式和后续事项。
- 示例说明要包含运行命令、依赖环境、输入输出和已知限制。
- 跨项目通用内容放根 `docs/`，项目专属内容放项目内 `docs/`。

## 代码约定

- 示例代码优先小而完整，能直接运行。
- 可复用逻辑沉淀到项目内 `src/`，不要长期停留在 notebook 或一次性脚本里。
- 引入新依赖时，同步更新依赖说明或配置模板。
- 对解析、状态流转、工具调用、RAG 分块、Agent 控制流等容易回归的逻辑，应补充聚焦测试。
- 不为了复用而过早抽象；当两个以上示例稳定复用同一逻辑时，再考虑下沉公共模块。

## AI Agent 实验约定

记录 Agent 实验时，至少说明：

- Agent 的目标和适用场景。
- 可使用的工具。
- 输入、输出和状态结构。
- 失败模式、权限边界和停止条件。
- 如何运行，以及如何判断结果是否符合预期。

Prompt 资产应优先放在项目内 `prompts/`。如果多个项目都使用同一类 Prompt，再考虑在根 `docs/` 中写通用说明。

## Git 与协作

- 修改前先检查 `git status --short`。
- 不回滚他人已有改动，除非用户明确要求。
- 不在未确认的情况下执行破坏性命令。
- 提交前尽量保持变更范围清晰：文档、示例、配置、代码分开说明。

